<Window x:Class="TaskTuner.Views.NewTaskWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TaskTuner.Views"
        mc:Ignorable="d"
        Title="NewTaskWindow" WindowStartupLocation="CenterScreen" Width="620">
    <Grid Background="{StaticResource TaskListBackGround}">
        <Grid.RowDefinitions>
            <RowDefinition Height="40" />
            <RowDefinition Height="200" />
            <RowDefinition Height="40" />
            <RowDefinition Height="250" />
            <RowDefinition Height="40" />
            <RowDefinition Height="40" />
            <RowDefinition Height="40" />
            <RowDefinition Height="1*" />
        </Grid.RowDefinitions>
        <!-- Task Title -->
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="450" />
                <ColumnDefinition Width="150" />
            </Grid.ColumnDefinitions>
            <TextBox Grid.Column="0" Style="{StaticResource FormTextBox}" Width="Auto"  />
            <ComboBox Grid.Column="1" Style="{StaticResource FormCombo}" HorizontalAlignment="Right" Width="150"></ComboBox>
        </Grid>
        

        <!-- Task Description -->
        <TextBox Grid.Row="1" Style="{StaticResource FormTextBox}" 
                 AcceptsReturn="True" TextWrapping="Wrap" 
                 VerticalScrollBarVisibility="Auto" />
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="550" />
                <ColumnDefinition Width="50" />
            </Grid.ColumnDefinitions>
            <TextBox Grid.Column="0" Style="{StaticResource FormTextBox}" Width="Auto"  />
            <Button Grid.Column="1"  HorizontalAlignment="Right" Margin="0 0 10 0" Width="36" >
                <Viewbox Width="20" Height="20">
                    <Path Data="{StaticResource TaskListIcon}" Fill="#000"></Path>
                </Viewbox>
            </Button>
        </Grid>
        
        <TextBox Grid.Row="3" Style="{StaticResource FormTextBox}" 
         AcceptsReturn="True" TextWrapping="Wrap" 
         VerticalScrollBarVisibility="Auto" />

        <!-- Due Date and Time -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" Margin="10,10,0,0">
            <TextBlock Text="Due:" VerticalAlignment="Center" Margin="0,0,5,0" />
            <DatePicker Width="120"/>
            <ComboBox Width="60" Margin="5,0" Style="{StaticResource FormCombo}">
                <!-- Hours -->
                <ComboBoxItem Content="1"/>
                <ComboBoxItem Content="2"/>
                <ComboBoxItem Content="3"/>
                <ComboBoxItem Content="4"/>
                <ComboBoxItem Content="5"/>
                <ComboBoxItem Content="6"/>
                <ComboBoxItem Content="7"/>
                <ComboBoxItem Content="8"/>
                <ComboBoxItem Content="9"/>
                <ComboBoxItem Content="10"/>
                <ComboBoxItem Content="11"/>
                <ComboBoxItem Content="12"/>
            </ComboBox>
            <ComboBox Width="60" Margin="5,0" Style="{StaticResource FormCombo}">
                <!-- Minutes -->
                <ComboBoxItem Content="00"/>
                <ComboBoxItem Content="15"/>
                <ComboBoxItem Content="30"/>
                <ComboBoxItem Content="45"/>
            </ComboBox>
            <ComboBox Width="60" Margin="5,0" Style="{StaticResource FormCombo}">
                <!-- AM/PM -->
                <ComboBoxItem Content="AM"/>
                <ComboBoxItem Content="PM"/>
            </ComboBox>
        </StackPanel>

        <!-- Options -->
        <CheckBox Grid.Row="5" Margin="10,10,0,0"
                  Content="Include Timer (You can time how long a project takes with a stop watch.)"/>
        <CheckBox Grid.Row="6" Margin="10,5,0,0"
                  Content="Add task to your calendar"/>
        <Button Grid.Row="7" Style="{StaticResource CompleteButton}">Add Task</Button>

    </Grid>
</Window>
